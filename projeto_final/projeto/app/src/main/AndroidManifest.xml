<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto_PAC">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecyclerViewTest">
        <activity android:name=".AddNewDonation"></activity>
        <activity android:name=".DetailsDonation" />
        <activity android:name=".DonationActivity" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".addNewFamily" />
        <activity android:name=".addNewCompany" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>